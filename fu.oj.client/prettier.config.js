/** @type {import("prettier").Config} */
const prettierConfig = {
    quoteProps: "consistent",
    tabWidth: 4, // Kích thước tab
    useTabs: false, // Sử dụng space thay vì tab
};

export default prettierConfig;
